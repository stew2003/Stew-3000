(executable
  (name assemble)
  (modules assemble)
  (preprocess
    (pps ppx_let))
  (libraries core asm util errors))

(executable
  (name emulate)
  (modules emulate)
  (preprocess
    (pps ppx_let))
  (libraries core asm util emulator errors))

(library
  (name errors)
  (modules errors)
  (libraries core util))

(env
  (dev
    (flags (:standard -warn-error -A))))