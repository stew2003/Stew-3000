(executable
  (name main)
  (modules main)
  (libraries asm))

(executable
  (name assemble)
  (modules assemble)
  (preprocess
    (pps ppx_let))
  (libraries core asm))

(executable
  (name emulate)
  (modules emulate)
  (preprocess
    (pps ppx_let))
  (libraries core asm emulator))

(env
  (dev
    (flags (:standard -warn-error -A))))